<nav class="navbar navbar-expand-lg navbar-dark bg-info">
    <div class="container-fluid">
        <a class="navbar-brand" href="#">Fn Store</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                <li class="nav-item">
                    <a class="nav-link active" href="@Url.Action("Index","Home")">Pagina Inicial</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link active" href="@Url.Action("Index","Produto")">Produtos</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link active" href="@Url.Action("Sobre","Home")">Sobre</a>
                </li>


            </ul>

            <ul class="navbar-nav ml-auto mb-2 mb-lg-0">

                @if (User.Identity.IsAuthenticated)
                {
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                            @User.Identity.Name
                        </a>
                        <div class="dropdown-menu">
                            <a class="dropdown-item" href="#">Trocar Senha</a><
                            <a class="dropdown-item" href="#">Meu Perfil</a><


                            <div class="dropdown-divider" href="#"></div>


                            <a class="dropdown-item" href="@Url.Action("LogOut","Conta")">LogOut</a>
                        </div>

                    </li>

                }
                else
                {
                    <li class="nav-item">
                        <a class="nav-link active" href="@Url.Action("Login","Conta")">Efetuar Login</a>
                    </li>
                }
            </ul>

            

        </div>
    </div>
</nav>

